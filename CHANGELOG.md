# 更新日志

本项目的所有显著变更都将记录在此文件中。

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 约定，文件格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

本文件的内容基于对代码实际差异的比对（而非提交信息），以准确反映用户可见的行为变化。

## [v1.0.1]

### 变更

- 页面列表缩略图改为按需生成：打开文件或文档后不再逐页补齐，只有页面列表中被实现的页才渲染（列表默认收起，多数使用场景完全不渲染）。
- 文档页背景光栅化由 192 DPI 改为 144 DPI：单页位图内存由约 11-14 MB 降至 6-8 MB，放大到约 1.3 倍以上时可察觉清晰度差异。
- 文档页背景的显示、缩略图与导出统一按打开或附加文档时画布可见区域的等比适配盒布置（位置与尺寸由同一盒子决定），不再按图像自身 DPI 折算的原始尺寸显示；导出图像与 PDF 中背景的位置与屏幕显示一致。
- 附加与打开文档时，文档背景直接布置在当前视口内，视图保持不动，换页往返后各文档页显示一致（原先背景布置在画布中心：打开与在空页上附加时跳转视图，在已有墨迹的页上附加时文档落在视口外不可见）。
- 附加 `.isf` 墨迹时，墨迹与当前可见区域不完全重合即平移到视口中心（与粘贴落点同一规则），保证附加后可见（原先按文件坐标原样落地，可能完全落在视口外）。
- 打开 `.isf` 墨迹后墨迹居中显示在当前视口内，视图保持不动（原先固定跳回初始视口，墨迹可能落在视口外不可见）。
- 文档页图像按最近使用缓存 10 页：上一页／下一页来回翻不再重复光栅化。
- 自动备份改为内容未变化时跳过（只翻页或长时间停留不再产生文件），并在后台线程写盘；未处理异常路径仍同步写完。
- 画布内容启用位图缓存（`BitmapCache`）：平移与滚动不再逐帧重绘画布，缩放与笔画编辑时缓存失效、按原路径重绘。
- 右侧页面列表面板的开合尺寸改为按内容实测：展开宽度取页面列表需求宽与按钮行宽的较大值（原先固定加 64），展开后高度到窗口底部（原先减 10），收起高度取按钮行高度（原先固定 55）；快速连点后终态与最终选择一致，窄视口下缩略图列不再被视口压缩。
- 折叠画布两侧工具栏时，页面列表面板若处于展开状态先一并收起；折叠后屏幕上保留折叠与上页按钮，其余按钮移出屏幕，位移按保留区实测（原先固定 87 像素）；快速连点不再产生位移跳变。
- 时间文字颜色由 `#66FFFFFF` 改为 `#AACCCCCC`；菜单勾选对勾加粗并提高不透明度（`#CCEEEEEE` → `#DDEEEEEE`，暗底对勾 `#CC333333` → `#DD111111`）。
- 调试浮层：参数可视化去掉说明文案，改为「名称 = 值」标注；运行状态计数分列屏幕左右两侧（触点、笔画、撤销、重做），并新增帧率与最长帧读数，面板头部显示 `RenderCapability.Tier` 与 `ProcessRenderMode`（帧率口径与实测见 `docs/FrameRate.md`）。
- 调试浮层：`PinchLockDistance` 与 `PanZoomDisplaceThreshold` 取超出屏幕尺度或非正值（表示关闭）时显示「禁用」，不再打印原始数值。
- 关于对话框版本号由 `v1.0.0` 改为 `v1.0.1`。

## [v1.0.0]

### 新增

- 「新建」菜单：清空当前墨迹与文档视图，回到单张空白页。
- 「附加」菜单：把内容追加到当前白板。`.lbf` 的页追加到末尾并切到首张新页，`.isf` 墨迹追加到当前页，文档与图片在未打开文档时作为背景附加（页数补足到文档页数，已有墨迹的页保留）；已打开文档时按替换式打开处理。
- 「调试」菜单：勾选后在画布下方居中显示参数可视化（半径与距离的 1:1 圆／线段标注、缩放范围对数轴、平滑死区轴）。
- 导出进度：导出图片与导出 PDF 期间显示确定进度条，文案为「正在导出图片 3/10」形式；PDF 写入阶段单独作为一步上报。
- 画布尺寸按主屏分辨率派生（宽 4 屏、高 16 屏），初始视口距画布左／上边缘各一屏（原先为固定 32768 × 16384 且从画布中心开始）；`InkCanvasNext` 可传入画布尺寸与初始偏移。
- 平移或缩放结束、视口距右／下边缘不足一屏时，画布自动扩展一屏。
- 打开 `.lbf` / `.isf` 时笔画越出画布边界（其他分辨率设备产生的文件）会自动扩展画布。
- 再次启动已运行的实例并带文件参数时，唤起既有窗口并打开该文件。

### 变更

- 笔宽档位由 4 档增至 6 档（1 / 2 / 3 / 4 / 5 / 10）。
- 调色板两色调整：`#D7BA7D` → `#F99739`，`#569CD6` → `#2E7AE6`。
- 直线／圆形模式下再点当前已选中的颜色或粗细即回到画笔；点其他颜色或粗细只换笔、保持形状。
- 透明模式的鼠标穿透区由 48 × 480 改为 64 × 640，边框由 1 像素 `#33FFFFFF` 改为 2 像素 `#EEEEEE`，向内收缩量由 5 像素改为 2 像素。
- 主题调整：容器底色与描边色重调（`#99132032` / `#66DDEEFF`），图标字号 24 → 27（选中态 24），颜色与粗细选中时显示对勾，投影参数微调。
- 菜单项布局改用 `DockPanel`，补上禁用态（透明底色 + 35% 不透明度）与勾选态（`DarkBrush` 底色 + 描边）。
- 加载浮层文案随操作变化（「正在打开文档」「正在附加文档」），不再固定显示「正在加载中…」。
- 导出目录名由 Ticks 改为 `yyyyMMddHHmmss`。
- 图片导出中空白页不写文件，但仍上报进度，进度条连续推进。
- PDF 导出期间禁用画布，与图片导出对齐。
- 未处理异常路径下的自动备份不再吞掉异常。
- 关于对话框版本号由 `v1.0.0 RC` 改为 `v1.0.0`。

### 修复

- 修复 `.lbf` 打开时报 `InkSerializedFormat 操作失败`：ZIP 条目流不可 seek，WPF 对非 seekable 流按「读到短块即停」拷贝，超过一个缓冲区的墨迹被静默截断；改为先完整复制到内存流再解析。
- `.lbf` 增加版本与完整性校验：文件版本高于当前支持的版本时拒绝打开，页数据缺失时报「文件缺少第 N 页数据」；写入失败时清理临时文件。
- 修复双指手势下选区脱离手指：矩阵复合顺序错误（平移先于旋转与缩放生效），帧间增量应为左乘手势基线而非右乘。
- 修复选区双指手势的基线时序：手指抬落瞬间重建基线，不再吞掉当帧位移；一次手势只产生一条撤销记录。
- 修复形状绘制中第二指落入后形状终点被其他手指接管：形状终点恒取插入序第一指，其余手指的移动与抬手被忽略，第一指抬起时按其触点提交。
- 修复拖到画布右／下边缘后无法继续平移的问题：手势结束时补足边界余量。
- 修复无边框最大化窗口下打开文档时视图偏移偏差：改为取画布真实视口尺寸居中（原先按 `SystemParameters.WorkArea` 估算）。
- 修复测试宿主「测试全过但退出码 1」：进程退出时 WPF 对已退场 STA 线程的 Dispatcher 调用 `WeakEventTable.OnShutDown` 会无限阻塞、卡死 CLR 关停序列；按 WPF 官方开关跳过该 Invoke。

## [v1.0.0-rc.4]

### 新增

- 「导出 PDF」：把全部页面导出为单个 PDF，可选 25% / 50% / 100% 渲染倍率；导出为白底，文档页作为背景与墨迹一并输出，页面物理尺寸按显示 DPI 折算为点。
- 透明模式新增鼠标穿透区：窗口左侧中部的 48 × 480 矩形不接收鼠标操作，点击落到其下方窗口。
- 选区新增旋转手柄：浮在选区上沿正上方固定屏幕间距处，拖动绕选区中心旋转选中墨迹；视觉半径与命中半径不随画布缩放变化。
- 打开文档后缩略图在后台逐页补齐，从空白逐步变为带页面图像。
- 新增自动化测试工程（触摸状态机、几何、选区视觉、工具栏交互、页面模型、PDF 导出）与 CI 测试作业。

### 变更

- 目标框架由 .NET 9 升级到 .NET 10；框架依赖发布需目标机安装 .NET 10 桌面运行时。
- 界面改为深色半透明容器风格：工具栏改为半透明深色底 + 1 像素浅色描边与投影，按钮常态透明、悬停与按下高亮，选中的工具与色块改用蓝色高亮描边。
- 画布底色由纯黑改为 `#141414`，网格线颜色由 `#222222` 改为 `#232323`。
- 直线／圆形不再是独立开关，与线擦、面积擦、选择同属排他单选组；再次点击已选中的工具回到画笔（擦除与选择同样适用）。
- 进入擦除或选择模式时取消颜色与笔宽的勾选，进入直线／圆形时保持勾选。
- 工具栏按钮顺序与间距调整：直线／圆形紧邻擦除与选择，粘贴按钮右移，透明模式按钮移到最右端，高度由 53 改为 54。
- 选区工具条由吸附在选区顶部居中改为吸附在包围盒左下角，选区下方空间不足时移到选区上方。
- 右侧页面预览由 `ListBox` 改为 `ListView` + `GridView`：缩略图宽度由 170 改为 225，选中页由蓝色填充改为蓝色描边加粗页码，列表隐藏滚动条并支持触摸纵向拖动滚动。
- 页面面板展开与收起动画参数调整：展开更高（`ActualHeight - 10`）且宽度增加 64，收起高度由 48 改为 55、宽度减少 64。
- 透明模式开启与关闭时同步设置窗口置顶。
- 打开文档的加载文案由「解析文档中…」改为「正在加载中…」。
- 点击粘贴按钮且剪贴板含墨迹时，先退出高亮并切回画笔，再进入粘贴印章；剪贴板无墨迹时不扰动当前模式。
- 关闭窗口的未保存提示在画布已无任何墨迹时不再弹出。
- 双指缩放期间只剩一指时转入单指平移，此后落下第二指也不再恢复缩放，需全部抬起重新开始。
- 双指平移与缩放互斥：两指中点整体位移超过 30 像素，或两指距离小于 24 像素时暂停缩放，只保留平移。
- 选区手势期间允许附加手指：3 或 4 指并拢转为平移画布，5 指及以上并拢转为掌压擦除。
- 导出图片时把当前文档页作为背景与墨迹一并导出。
- 打开图片与 PDF 不再派发到后台线程解析，改在调用线程同步执行，打开大体积 PDF 时界面可能短暂无响应。
- 打开 PDF 失败的提示附带十六进制错误码（「无法打开 PDF 文件：XXXX」）。

### 修复

- 修复双指旋转选区时旋转量几乎不可见：弧度值被直接当作角度传入，实际旋转角度缩小约 57 倍。
- 修复绘制直线／圆形过程中落下第二指导致平移缩放失效：形状仅在单指绘制上下文存活，一旦转入平移、缩放、多指、擦除或选区即放弃未提交的形状预览。
- 修复拖动选区时第二指落下后缩放与旋转存在死区：改为在第二指落下的瞬间结束移动段并采集捏合基线。
- 修复多页导出时空白页输出为 1 × 1 像素图片：无墨迹时按 1920 × 1080 兜底画布导出。
- 修复从高亮笔退回画笔时工具栏勾选状态未同步。
- 修复异常详情对话框的「复制」在剪贴板被占用时静默失败。

### 移除

- 删除内部设计文档 `docs/EraserReference.md`、`docs/MultiTouchReference.md`。

[v1.0.1]: https://github.com/KaiHuaDou/DrawingNotepad/compare/v1.0.0...v1.0.1
[v1.0.0]: https://github.com/KaiHuaDou/DrawingNotepad/compare/v1.0.0-rc.4...v1.0.0
[v1.0.0-rc.4]: https://github.com/KaiHuaDou/DrawingNotepad/compare/v1.0.0-rc.3...v1.0.0-rc.4
